# Whisper 微调依赖
transformers>=4.30.0
datasets>=2.14.0
librosa>=0.10.0
evaluate>=0.4.0
jiwer>=3.0.0
tensorboard>=2.14.0
accelerate>=0.21.0
peft>=0.5.0  # LoRA 微调支持

# jieba>=0.42.0  # 已移除：中文 ASR 使用 CER（按字）而非 WER（按词）